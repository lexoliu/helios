[package]
name = "helios-compositor-render"
version = "0.1.0"
edition.workspace = true

[dependencies]
helios-api = { path = "../../../api", default-features = false, features = [
    "display",
    "input",
] }
noto-sans-mono-bitmap = { version = "0.3.2", default-features = false, features = [
    "regular",
    "size_16",
    "unicode-basic-latin",
] }

[lints]
workspace = true
