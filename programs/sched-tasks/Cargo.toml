[package]
name = "helios-sched-tasks"
version = "0.1.0"
edition.workspace = true
autobins = false

[dependencies]
helios-api = { path = "../../api", default-features = false, features = ["channel", "task"] }
helios-bench-metrics = { path = "../../tools/wasi-apps/bench-metrics" }
thiserror = { version = "2.0.17", default-features = false }

[lib]
name = "helios_sched_tasks"
path = "src/main.rs"
crate-type = ["cdylib"]
