[package]
name = "helios-perf"
version = "0.1.0"
edition.workspace = true
autobins = false

[dependencies]
helios-api = { path = "../../api", default-features = false, features = ["instances", "profiling"] }

[lib]
name = "helios_perf"
path = "src/main.rs"
crate-type = ["cdylib"]

[lints]
workspace = true
