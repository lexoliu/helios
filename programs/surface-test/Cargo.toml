[package]
name = "helios-surface-test"
version = "0.1.0"
edition.workspace = true
autobins = false

[dependencies]
helios-api = { path = "../../api", default-features = false, features = ["surface", "task"] }
thiserror = { version = "2.0.20", default-features = false }

[lib]
name = "helios_surface_test"
path = "src/main.rs"
crate-type = ["cdylib"]

[lints]
workspace = true
