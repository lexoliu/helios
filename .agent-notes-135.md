# Issue #135 — kernel/ single-use helper cleanup

Legend: INLINE = absorbed into its caller; KEEP = defended, with the reason.

## kernel/build.rs
- generate_embedded_init — KEEP (separates cargo-env plumbing from template generation; 37 lines)
- read_kernel_prebuild_manifest — KEEP (also emits every rerun-if-changed; the manifest contract)
- absolutize_prebuild_manifest_paths — KEEP (states the rule: manifest paths are relative to the manifest)
- write_trusted_root_file — KEEP (build-time key embedding + 32-byte assertions)
- embedded_bootfs_entries — KEEP (66 lines)
- rerun_if_changed_recursive — KEEP (name states the rule)

## kernel/src/bootfs.rs
- push_boot_directory_entry — INLINE (dedup-then-push; the loop reads better with it in place)
- directory_child_path — KEEP (path join with an intentional with_capacity)
- directory_entry_exists — KEEP (states "already listed" across both DirectoryEntry arms)
- taste: dropped a stray `;` after an `if { continue; }` block

## kernel/src/component/
- cache.rs evict_to_budget — KEEP (eviction policy; unit-tested module)
- runtime.rs name_user_memory_owner — KEEP (doc comment states the page-charging rule; hot path)

## kernel/src/exec/executor.rs — all KEEP
- take_block, push_free, pop_ready — lock-free arena/queue internals with invariant doc comments
- global_scheduler / local_scheduler / local_silent_scheduler — the three scheduler views, one per scheduler type
- wake_one_remote_processor — #[inline] SMP hot-path split
- executor_group — one-time global group construction

## kernel/src/exec/observer.rs — all KEEP
- parse_console_line, split_prefixed_line (the "[target] message" format rule), level_priority
  (the level ordering rule, used twice on one line), strip_ansi

## kernel/src/exec/sync.rs
- add_permits — KEEP (lock-free CAS loop, mirrors try_claim_permit)

## kernel/src/exec/timer.rs — all KEEP
- drain_inbox, publish_deadline, commit_deadline — inbox/hardware-arming invariants
- observe_deadline, rebuild_next_deadline, cascade, cascade_level, wheel_level — timing-wheel vocabulary
- taste: publish_deadline's `match cas { Ok => break, Err => continue }` is now `if cas.is_ok() { break }`

## kernel/src/runtime/state.rs
- the twelve `*_delta` wrappers over swap_delta/heap_size_class_delta — INLINE (each was one
  delegating line used once; record_kernel_heap_metrics now calls swap_delta/heap_size_class_delta
  on the snapshot fields directly, in the same order)
- heap_size_class_metric_name — KEEP (the metric-name table)

## kernel/src/runtime/types.rs
- write_decimal_u8 — KEEP (no_std itoa the SAFETY comment above it names)

## kernel/src/log.rs — all KEEP
- try_flush / flush_queue — the single-flusher invariant flush_queue's SAFETY comment names
- format_event — named formatting step

## kernel/src/lib.rs — all KEEP
- record_dealloc / record_realloc — GlobalAlloc hot path, mirror record_alloc
- spawn_watchdog_supervisor, spawn_watchdog_self_test, watchdog_self_test_delay — watchdog policy
- init_allocator — bootstrap kernel/user memory split
- wait_for_bootstrap — the SMP boot barrier

## kernel/src/memory/
- frame_slab.rs try_reserve_cached_frame — KEEP (lock-free reservation, unit-tested module)
- frame_slab.rs shard_capacity_frames — KEEP (per-shard capacity rule)
- frame_slab.rs slab_capacity_frames — INLINE (one-line div_ceil; the whole capacity rule now
  reads as one chain in shard_capacity_frames)
- balloon.rs inflation_budget — KEEP (doc states the OOM pressure floor)
- entropy.rs read_at_bring_up — KEEP (doc justifies the pre-executor poll loop)
- reservations.rs committed_regions_cover — KEEP (coverage walk, names the invariant)
- swap.rs report_activity — KEEP (doc states the report rate limit)
- swap.rs current_pressure — KEEP (doc states which pool pressure is read from)

## kernel/src/io/
- block.rs corrupting — KEEP (test fixture constructor)
- child.rs push_if_open — KEEP (holds the chunks lock across the closed check)
- child.rs register_waker — KEEP (waker registry invariant)
- console.rs write_fmt — KEEP (fmt::Write override; the doc is the console-gate rule)
- console.rs kernel_logger — KEEP (test helper)
- iommu.rs merge_touching — KEEP (algorithm step)
- iommu.rs fold_to — KEEP (doc states the window-budget folding policy)

## kernel/src/instance.rs — all KEEP
- noop_kill_notifier — the default `fn()` notifier
- open_activation — paired with close_activation; doc states the single-owner invariant

## kernel/src/host_fs/cache.rs
- is_normalised — KEEP (doc states the cache-key spelling rule)

## kernel/src/vsock/table.rs — all KEEP
- handle_request/response/reset/shutdown/data/credit_update/credit_request — the per-op dispatch table
- announce, at_eof — protocol rules stated in their docs
- listener_mut — mirrors `listener`

## kernel/src/network/
- http/mod.rs is_field_value, is_uri_scheme — KEEP (RFC grammar rules)
- http/mod.rs entry_bytes_for — INLINE (`set` now computes `removed` and `added` side by side,
  which is the pair the size check is about)
- service/shard.rs flow_tuple_for_frame, is_dhcp_exchange, icmpv4_quoted_flow,
  icmpv6_quoted_flow — KEEP (hot RX path; each doc states a steering/quoting rule)
- service/tcp.rs reclaim_transmit_completions, receive_frames_immediate — KEEP (multi-queue SMP
  invariants, long docs)
- service/mod.rs reset_link_configuration, local_address_for, has_ipv6_resolver,
  synchronize_link_state — KEEP (docs state link/DNS policy)
- service/mod.rs update_neighbors — KEEP (the ArcSwap copy-on-write publish rule)
- service/mod.rs list_ipv4_routes — KEEP (named projection to the WIT route type)
- service/mod.rs listen_tcp — KEEP (test helper)
- service/mod.rs event_queue_idx — gone on current dev, nothing to do
- service/mod.rs record_network_profile_between — INLINE (pure delegation adding `0, 0`; the two
  other call sites already call record_network_profile_events_bytes_between directly)

## kernel/src/wasmtime_adapter/ (outside component_host/mod.rs and wasi/http.rs) — all KEEP
- artifact_profile.rs collect_core_imports, classify_core_module, component_profile,
  is_wasi_import_version — artifact classification rules
- config.rs relaxed_simd_module, relaxed_simd_module_operator_set — test helpers
- engine.rs build_engine_for_platform, apply_pooling_config — the AGENTS §3.2 engine opt-ins
- mod.rs interpret_run_result — doc states the wasi:cli/exit trap rule
- store.rs try_satisfy_or_kill — the two-pool OOM policy
- wasi/fs.rs start_host_read, start_host_write — bounded-chunk streaming rules
- wasi/mod.rs readonly_root_descriptor, wit_scoped_function, wit_scoped_resource_function — test helpers
- wasi/preview1.rs witx_exports — test helper
- wasi/preview2.rs subscribe_instant, p2_usize_to_u64, format_p2_tcp_family, format_p2_ip_address,
  map_p2_dns_error, descriptor_type_from_kind, descriptor_flags_from_p3, descriptor_flags_to_p3,
  open_flags_to_p3, path_flags_to_p3, directory_entry_from_p3 — p2/p3 WIT type mappings
- component_host/service/mod.rs system_component_profile_stack, kernel_processor_profile_stack,
  load_precompiled_executable, load_precompiled_core_module, deserialize_component,
  invoke_compiler_inner, read_compiler_plugin_artifact, ensure_compiler_plugin — compiler-plugin
  provisioning and artifact loading
- component_host/service/preview1.rs p1_read_subscriptions, regular_file, connected_socket
- component_host/service/exec.rs caused_by_out_of_memory — doc + #62
- component_host/service/wasix_net.rs wasix_socket_backend_handle
- taste: fs.rs FileWriteMode is now `#[derive(Clone, Copy)]`, so start_host_write copies it
  instead of rebuilding it arm by arm
- taste: ensure_compiler_plugin's three-arm match whose two failing arms built the same error is
  now one `matches!` guard

## kernel/src/wasmtime_adapter/component_host/mod.rs — all 38 KEEP
Every one is a WIT binding shape: `add_*_to_linker` wires a generated world into a `Linker`, and
`convert_*` / `unavailable_*` map a kernel type onto the debugger world or the program world.
- taste: the file spelled `debugger_bindings::helios::system::…` 146 times and
  `program_bindings::helios::system::…` 111 times. Both worlds now have a `debugger_wit` /
  `program_wit` alias at the `helios::system` level, which lets rustfmt fit most match arms and
  struct literals on one line: -177 lines with no change to what the file does.

## kernel/src/wasmtime_adapter/wasi/http.rs — all 26 KEEP
`get_*` / `set_*` are the generated `wasi:http/types` host-trait methods (the generated dispatch
is their single caller) and `wit_*` / `kernel_*` map a kernel type onto the WIT type.

## kernel/src/network/service/component.rs — all KEEP
- resolver_endpoint, open_resolver_socket, close_resolver_socket — the DNS socket lifecycle;
  each doc states a shard-affinity or port-leak rule
- answered_any — pairs with `complete()` over the A/AAAA answer states

## kernel/src/host_fs/client.rs
- rgetattr_body — KEEP (test helper)

## Totals
211 listed. 16 inlined, 194 kept, 1 (`event_queue_idx`) already gone on dev.
