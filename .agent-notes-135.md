# Issue #135 — backend single-use helper cleanup

Base: origin/dev @ ea91c65 (includes PR #139).
Branch: taste/backends-single-use, worktree /Users/lexoliu/Coding/helios-wt-taste-backends

## Decisions log

### hosted (13 listed)
Inlined 4: `default_processor_count` (config.rs), `install_panic_hook` (lib.rs),
`invalid_host_path`, `truncate_impl` (host_fs.rs, a pure wrapper over `set_file_size_impl(_, 0)`).
Kept 9:
- `stat_impl` — one of a uniform family of `*_impl` blocking-pool bodies handed to `blocking_path`.
- `init_serial` / `as_memory_region` — the constructor/accessor of the SERIAL_* statics and of `HeapReservation`.
- `spawn_processor_thread`, `spawn_timer_thread` — 40-85 line thread bodies.
- `wait_for_processor_registration` — named boot phase in `run()`.
- `restore_committed` — doc comment states the rollback rule, not the body.
- `host_page_size` — wraps `unsafe sysconf`.
- `page_flags_to_prot` — PageFlags -> PROT translation table.
Checks: just check-host, clippy -p helios-hosted --all-targets -D warnings, test -p helios-hosted --lib (63 passed).
