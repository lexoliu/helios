name: CI

on:
  pull_request:
  push:
    branches: [dev]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  checks:
    name: ${{ matrix.name }}
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: check-host
            command: just check-host
          - name: check-aarch64
            command: just check-target aarch64-unknown-none helios-aarch64
          - name: check-riscv
            command: just check-target riscv64gc-unknown-none-elf helios-riscv
          - name: check-x86
            command: just check-target x86_64-unknown-none helios-x86
          - name: test-embedded-debugger
            command: just test-embedded-debugger
    steps:
      - uses: actions/checkout@v4
        with:
          path: helios
      - uses: ./helios/.github/actions/checkout-wasmtime
      - name: Install host dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libudev-dev just cmake ninja-build
      - uses: taiki-e/install-action@v2
        with:
          tool: wasm-tools
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.13.0
      - name: Restore WASI tooling artifacts
        id: artifact-cache
        uses: actions/cache@v4
        with:
          path: |
            helios/artifacts/wasi-tools
            helios/artifacts/python3-root
            helios/artifacts/wasix
          key: wasi-artifacts-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('helios/tools/wasi-apps/**', 'helios/Cargo.lock') }}
      - name: Stage WASI tooling artifacts
        if: steps.artifact-cache.outputs.cache-hit != 'true'
        working-directory: helios
        run: tools/wasi-apps/build.sh
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: helios
          key: ${{ matrix.name }}
      - name: ${{ matrix.name }}
        working-directory: helios
        run: ${{ matrix.command }}

  smoke-riscv64:
    name: smoke-riscv64
    runs-on: ubuntu-24.04
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
        with:
          path: helios
      - uses: ./helios/.github/actions/checkout-wasmtime
      - name: Install host dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libudev-dev just cmake ninja-build \
            qemu-system-misc opensbi
          qemu-system-riscv64 --version
      - uses: taiki-e/install-action@v2
        with:
          tool: wasm-tools
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.13.0
      - name: Restore WASI tooling artifacts
        id: artifact-cache
        uses: actions/cache@v4
        with:
          path: |
            helios/artifacts/wasi-tools
            helios/artifacts/python3-root
            helios/artifacts/wasix
          key: wasi-artifacts-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('helios/tools/wasi-apps/**', 'helios/Cargo.lock') }}
      - name: Stage WASI tooling artifacts
        if: steps.artifact-cache.outputs.cache-hit != 'true'
        working-directory: helios
        run: tools/wasi-apps/build.sh
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: helios
          key: smoke-riscv64
      - name: Build inspector
        working-directory: helios
        run: cargo build --release -p helios-inspector
      - name: Boot riscv64 guest
        shell: bash
        working-directory: helios
        env:
          # GitHub runners expose no riscv64 accelerator, so the guest runs
          # under TCG and needs an emulation-sized bring-up budget.
          HELIOS_BOOT_SYNC_TIMEOUT_SECS: "3600"
        run: |
          ./target/release/helios-inspector vm --arch riscv64 --release --memory 2G \
            --boot-program dash --boot-program debugger --no-compiler-plugin \
            shell -c 'echo ok' > boot-smoke.out
          cat boot-smoke.out
          grep -Fqx ok boot-smoke.out
      - name: Fetch over virtio-net with curl
        shell: bash
        working-directory: helios
        env:
          HELIOS_BOOT_SYNC_TIMEOUT_SECS: "3600"
        run: |
          # curl speaks wasi:http, which the kernel forwards to the
          # http-client plugin, so both boot programs are required. curl is
          # invoked by its bootfs path because a PATH-resolved exec currently
          # reaches the guest with argv[0] duplicated.
          ./target/release/helios-inspector vm --arch riscv64 --release --memory 2G \
            --boot-program dash --boot-program debugger \
            --boot-program curl --boot-program http-client --no-compiler-plugin \
            shell -c '/bin/curl http://detectportal.firefox.com/success.txt' > curl-smoke.out
          cat curl-smoke.out
          grep -Fqx success curl-smoke.out

  bench:
    name: bench-${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: x86-64-linux
            runner: ubuntu-24.04
            helios-arch: x86-64
            # Without lazy-commit virtual memory the x86 kernel allocates
            # WASIX shared-memory capacity eagerly, so the shell workloads
            # need more physical guest RAM than the aarch64 lanes.
            vm-memory: 6G
            # KVM boots in seconds; a marker hang should fail fast.
            boot-timeout: "300"
            workloads: ""
          - name: aarch64-linux
            runner: ubuntu-24.04-arm
            helios-arch: aarch64
            vm-memory: 2G
            boot-timeout: "3600"
            workloads: ""
          - name: aarch64-macos
            runner: macos-15
            helios-arch: aarch64
            vm-memory: 2G
            boot-timeout: "3600"
            workloads: ""
    steps:
      - uses: actions/checkout@v4
        with:
          path: helios
      - uses: ./helios/.github/actions/checkout-wasmtime
      - name: Install host dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libudev-dev just cmake ninja-build \
            qemu-system-x86 ovmf qemu-system-arm qemu-efi-aarch64
          # GitHub's x86-64 runners expose /dev/kvm; the inspector picks
          # the accelerator by capability, this only fixes permissions.
          if [ -e /dev/kvm ]; then
            sudo chmod 666 /dev/kvm
            ls -la /dev/kvm
          fi
      - name: Install host dependencies (macOS)
        if: runner.os == 'macOS'
        run: brew install qemu just cmake ninja
      - uses: taiki-e/install-action@v2
        with:
          tool: wasm-tools
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.13.0
      - name: Stage Limine bootloader share
        run: |
          git clone --depth 1 --branch v11.x-binary \
            https://github.com/limine-bootloader/limine "$GITHUB_WORKSPACE/limine"
          make -C "$GITHUB_WORKSPACE/limine"
      - name: Restore WASI tooling artifacts
        id: artifact-cache
        uses: actions/cache@v4
        with:
          path: |
            helios/artifacts/wasi-tools
            helios/artifacts/python3-root
            helios/artifacts/wasix
          key: wasi-artifacts-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('helios/tools/wasi-apps/**', 'helios/Cargo.lock') }}
      - name: Stage WASI tooling artifacts
        if: steps.artifact-cache.outputs.cache-hit != 'true'
        working-directory: helios
        run: tools/wasi-apps/build.sh
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: helios
          key: bench-${{ matrix.name }}
      - name: Install inferno flamegraph renderer
        run: cargo install inferno --locked
      - name: Build inspector
        working-directory: helios
        run: cargo build --release -p helios-inspector
      - name: Run Helios workload benchmarks
        working-directory: helios
        env:
          HELIOS_LIMINE_SHARE: ${{ github.workspace }}/limine
          HELIOS_LIMINE_BIN: ${{ github.workspace }}/limine/limine
          HELIOS_BOOT_SYNC_TIMEOUT_SECS: ${{ matrix.boot-timeout }}
          HELIOS_WORKLOAD_BENCH_VM_MEMORY: ${{ matrix.vm-memory }}
        run: |
          python3 tools/wasi-apps/linux-gap-bench.py \
            --arch ${{ matrix.helios-arch }} \
            --skip-linux \
            --allow-busy-host \
            --helios-timeout-seconds 9000 \
            --out-dir target/perf-baselines/ci-${{ matrix.name }} \
            ${{ matrix.workloads }}
      - name: Summarize results
        if: always()
        working-directory: helios
        run: |
          python3 - <<'EOF' >> "$GITHUB_STEP_SUMMARY"
          import json
          from pathlib import Path

          out_dir = Path("target/perf-baselines/ci-${{ matrix.name }}")
          print("### Helios workload medians (${{ matrix.name }})\n")
          print("| workload | class | median (ms) | iterations (ms) |")
          print("| --- | --- | --- | --- |")
          for jsonl in sorted(out_dir.glob("helios*.jsonl")):
              for line in jsonl.read_text().splitlines():
                  row = json.loads(line)
                  if row.get("type") != "summary":
                      continue
                  iters = ", ".join(str(v) for v in row.get("elapsed_ms", []))
                  print(
                      f"| {row['workload']} | {row['class']} "
                      f"| {row['median_elapsed_ms']} | {iters} |"
                  )
          EOF
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: perf-baselines-${{ matrix.name }}
          path: helios/target/perf-baselines/ci-${{ matrix.name }}/**
          if-no-files-found: warn

  bench-linux-baseline:
    name: bench-linux-baseline-aarch64
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          path: helios
      - uses: ./helios/.github/actions/checkout-wasmtime
      - name: Install host dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libudev-dev just cmake ninja-build \
            qemu-system-arm qemu-efi-aarch64 qemu-utils genisoimage
          # ubuntu-24.04-arm exposes no /dev/kvm; the baseline selects TCG by
          # capability and provisions the guest with emulation-sized timeouts.
      - uses: taiki-e/install-action@v2
        with:
          tool: wasm-tools
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.13.0
      - name: Restore WASI tooling artifacts
        id: artifact-cache
        uses: actions/cache@v4
        with:
          path: |
            helios/artifacts/wasi-tools
            helios/artifacts/python3-root
            helios/artifacts/wasix
          key: wasi-artifacts-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('helios/tools/wasi-apps/**', 'helios/Cargo.lock') }}
      - name: Stage WASI tooling artifacts
        if: steps.artifact-cache.outputs.cache-hit != 'true'
        working-directory: helios
        run: tools/wasi-apps/build.sh
      - name: Restore pinned Fedora VM assets
        uses: actions/cache@v4
        with:
          path: |
            helios/target/perf-baselines/linux-vm/fedora-aarch64/Fedora-Cloud-Base-Generic-44-1.7.aarch64.qcow2
            helios/target/perf-baselines/linux-vm/fedora-aarch64/sources
            helios/target/perf-baselines/linux-vm/fedora-aarch64/tools
          key: fedora-vm-assets-aarch64-44-1.7-wasmtime-48.0.0
      - name: Run Linux baseline workloads
        working-directory: helios
        run: |
          python3 tools/wasi-apps/linux-gap-bench.py \
            --skip-helios \
            --iterations 3 \
            --allow-busy-host \
            --linux-vm-memory 4G \
            --linux-vm-smp 4 \
            --linux-vm-setup-timeout-seconds 5400 \
            --out-dir target/perf-baselines/ci-linux-baseline-aarch64
      - name: Summarize results
        if: always()
        working-directory: helios
        run: |
          python3 - <<'EOF' >> "$GITHUB_STEP_SUMMARY"
          import json
          from pathlib import Path

          summary = Path("target/perf-baselines/ci-linux-baseline-aarch64/summary.json")
          if not summary.is_file():
              print("### Linux baseline (aarch64) produced no summary.json\n")
              raise SystemExit(0)
          payload = json.loads(summary.read_text())
          print(f"### Linux baseline: {payload['linux_baseline']['kind']}\n")
          print(f"- Wasmtime floor: `{payload['wasmtime_linux_baseline'].get('release')}`")
          print(f"- Guest CPU: `{payload['linux_baseline'].get('qemu_cpu')}` "
                f"on `{payload['linux_baseline'].get('qemu_machine')}`\n")
          print("| workload | class | Linux native (ms) | Wasmtime-on-Linux (ms) |")
          print("| --- | --- | ---: | ---: |")
          for row in payload["workloads"]:
              native = row["linux_median_ms"]
              floor = row["wasmtime_linux_median_ms"]
              print(f"| {row['name']} | {row['class']} "
                    f"| {'n/a' if native is None else f'{native:.2f}'} "
                    f"| {'n/a' if floor is None else f'{floor:.2f}'} |")
          EOF
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: perf-baselines-linux-baseline-aarch64
          path: |
            helios/target/perf-baselines/ci-linux-baseline-aarch64/**
            helios/target/perf-baselines/linux-vm/fedora-aarch64/serial.log
          if-no-files-found: warn
