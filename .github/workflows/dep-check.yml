name: Dependency Check

on:
  schedule:
    - cron: "0 9 * * 1"
  workflow_dispatch:

permissions:
  issues: write
  contents: read

jobs:
  check:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          path: helios
      # The workspace resolves `../wasmtime` at manifest time, so the
      # pinned tree must sit next to the checkout exactly as in CI.
      - uses: ./helios/.github/actions/checkout-wasmtime
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: helios
      # `cargo outdated` copies the manifests into a temporary directory
      # before resolving, which severs the `../wasmtime` path dependency.
      # `cargo upgrade --dry-run` resolves the real workspace in place and
      # prints the upgrade table on stdout only when something is upgradable.
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-edit
      - name: Check Rust dependencies
        working-directory: helios
        run: |
          cargo upgrade --dry-run --incompatible allow --pinned allow > outdated.txt
          cat outdated.txt
      - name: Report
        working-directory: helios
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh label create "dependencies" --description "Dependency updates" --color "0075ca" 2>/dev/null || true

          if [ ! -s outdated.txt ]; then
            echo "No outdated dependencies"
            existing=$(gh issue list --label "dependencies" --state open --json number --jq '.[0].number' 2>/dev/null || true)
            if [ -n "$existing" ] && [ "$existing" != "null" ]; then
              gh issue close "$existing" -c "All dependencies are up to date."
            fi
            exit 0
          fi

          content=$(cat outdated.txt)
          body=$(cat <<BODY
          ## Outdated Dependencies

          \`\`\`
          ${content}
          \`\`\`

          _Last checked: $(date -u +%Y-%m-%dT%H:%M:%SZ) · updated weekly_
          BODY
          )

          existing=$(gh issue list --label "dependencies" --state open --json number --jq '.[0].number' 2>/dev/null || true)

          if [ -n "$existing" ] && [ "$existing" != "null" ]; then
            gh issue edit "$existing" --body "$body"
            echo "Updated issue #$existing"
          else
            gh issue create --title "Outdated dependencies" --body "$body" --label "dependencies"
            echo "Created tracking issue"
          fi
